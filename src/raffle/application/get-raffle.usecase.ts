import { IRaffle } from "../../interfaces";

export const getRaffleUseCase = async (): Promise<IRaffle> => {
//   const notes = await NoteRepository.getAll();

//   return {
//     ...notes,
//   };
};